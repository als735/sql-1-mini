// -- SELECT * FROM artist;
// -- SELECT first_name, last_name, country FROM employee; 
// -- SELECT name, composer, milliseconds
// -- FROM track
// -- WHERE milliseconds > 299000; 
// -- SELECT count(*)
// -- FROM track 
// -- WHERE milliseconds > 299000; 
// -- SELECT AVG(milliseconds) 
// -- FROM track 
// -- SELECT * FROM invoice 
// -- SELECT COUNT(*) FROM invoice 
// -- WHERE billing_country = 'USA'; 
// -- SELECT first_name FROM customer WHERE first_name LIKE 'A%'; 
// -- SELECT * FROM track ORDER BY milliseconds ASC LIMIT 10; 
// -- SELECT * FROM track ORDER BY milliseconds DESC LIMIT 20 ; 
// -- SELECT * FROM customer WHERE state = 'California' OR state = 'Washington'; 
// -- SELECT * FROM customer WHERE state IN ('California', 'Washington', 'Utah', 'Florida', 'Arizona');
// -- SELECT * FROM playlist; 
// -- SELECT * FROM playlist_track; 
// Insert an artist to the database
// Insert yourself as a customer to the database
// Find a list of all Playlists that start with Classical